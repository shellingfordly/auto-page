export enum SchemaId {
  Image,
  Div,
  Slideshow,
  Container,
  Notice,
  Text,
}

export enum AnimationName {
  Slide = "slide", // 滑动
  Fade = "fade", // 渐变
  Card = "card", // 卡片
}

export enum IndicatorPosition {
  Bottom = "bottom",
  Top = "top",
  Left = "left",
  Right = "right",
  Outer = "outer",
}

export enum IndicatorType {
  Line = "line",
  Dot = "dot",
  Slider = "slider",
  Never = "never",
}
